{% set embedded = embedded|default(false) %}
<button type="button"
        class="actionButton btn {% if type == 'primary' %}btn-primary{% else %}btn-default{% endif %} navbar-btn {% if embedded %}btn-sm{% endif %}"
        data-targetSrc="{{ tagetSrc }}">{{ displayName }}
</button>