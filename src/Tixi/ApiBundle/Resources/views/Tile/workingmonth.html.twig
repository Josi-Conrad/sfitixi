{{ form_start(form, {'attr':{'id':formId, 'class':'form-horizontal customForm'} }) }}
<div class="col-md-12">
    <div class="row text-right">
        {% for resolvedElement in children %}
            {% if 'formcontrol' in resolvedElement.viewIndentifiers %}
                {{ resolvedElement.rawData | raw }}
            {% endif %}
        {% endfor %}
    </div>
    <div class="row">
        <div class="col-md-8">
            <div class="row">
                <div class="form-group">
                    <div class="col-md-4">
                        {{ form_label(form.workingMonthMemo, null, {'label_attr': {'style':'width:100%', 'class':'col-md-12 control-label' }}
                        ) }}
                    </div>
                    <div class="col-md-8">
                        {{ form_widget(form.workingMonthMemo, { 'attr': {'class': 'form-control'} }) }}
                    </div>
                    <div class="error_message">{{ form_errors(form.workingMonthMemo) }}</div>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">
            <div class="row">
                <div class="form-group">
                    <div class="col-md-4">
                        {{ form_label(form.workingMonthDateString, null, {'label_attr': {'style':'width:100%', 'class':'col-md-12 control-label' }}
                        ) }}
                    </div>
                    <div class="col-md-2">
                        {{ form_widget(form.workingMonthDateString, { 'attr': {'class': 'form-control'} }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">
            <div class="row">
                <div class="form-group">
                    <div class="col-md-4">
                        {{ form_label(form.workingMonthStatus, null, {'label_attr': {'style':'width:100%', 'class':'col-md-12 control-label' }}
                        ) }}
                    </div>
                    <div class="col-md-2">
                        {{ form_widget(form.workingMonthStatus, { 'attr': {'class': 'form-control'} }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="col-md-12">
    <hr>
    <table class="table table-hover">
        <tr>
            <th>
                {% trans %}workingmonth.th.date{% endtrans %}
            </th>
            <th>
                {% trans %}workingmonth.th.weekday{% endtrans %}
            </th>
            <th>
                <table width="100%">
                    <tr>
                        {% for shiftName in form.workingShiftNames %}
                            <th>
                                {{ form_widget(shiftName.workingShiftName) }}*
                            </th>
                        {% endfor %}
                    </tr>
                </table>
            </th>
            <th>
                {% trans %}workingmonth.th.comment{% endtrans %}
            </th>
        </tr>

        {% for day in form.workingDays %}
            <tr>
                <td width="15%">
                    {{ form_widget(day.workingDayDateString) }}
                    {{ form_label(day.workingDayDateString, null, {'label_attr': {'class':'control-label' }}) }}
                </td>
                <td width="15%">
                    {{ form_widget(day.workingDayWeekDayString) }}
                    {{ form_label(day.workingDayWeekDayString, null, {'label_attr': {'class':'control-label' }}) }}
                </td>
                <td width="40%">
                    <table>
                        <tr>
                            {% for shift in day.workingShifts %}
                                <td>
                                    {{ form_widget(shift.workingShiftAmountOfDrivers) }}
                                    {{ form_label(shift.workingShiftAmountOfDrivers, null, {'label_attr': {'class':'control-label' }}) }}
                                </td>
                            {% endfor %}
                        </tr>
                    </table>
                </td>
                <td width="30%">
                    {{ form_widget(day.workingDayComment) }}
                </td>
            </tr>
        {% endfor %}
    </table>
</div>

{{ form_end(form) }}

<script type="text/javascript">
    jQuery(function ($) {
        new FormValidationController('{{ formId }}');
    });
</script>
