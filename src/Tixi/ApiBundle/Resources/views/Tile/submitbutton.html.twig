{% set embedded = embedded|default(false) %}
<input type="submit"
        class="submitButton btn {% if type == 'primary' %}btn-primary{% else %}btn-default{% endif %} navbar-btn {% if embedded %}btn-sm{% endif %}"
        data-targetFormId="{{ formId }}">{{ displayText }}
</input>